# 2026-04-17 · Kickoff

**Goal**: 현재 배포 상태 동기화 확인 + 대상자관리 통합 방향 잡기 + 장기 문서 체계 구축.

## Done

### 1. 배포·동기화 상태 점검
- **Git**: `main` @ `63777cc` — `origin/main` 완전 동기 (ahead 0 / behind 0)
- **Vercel**: production `dpl_2oye5NmVU8EUBfBcXN8Q7vX32dfi` @ `63777cc` (매치)
- **Supabase** (`cs-survey` / `gdwhbacuzhynvegkfoga`): 60 마이그레이션, 최신 `20260415234628_20260417_external_send_history`
- Prisma 로컬 스키마는 3 마이그레이션만 보유 → 실제 DB 57개 테이블과 드리프트 존재 (별도 `claude/romantic-lederberg` 브랜치에 "Prisma 전체 스키마 동기화 B안" 계획 문서)

### 2. `.claude/settings.local.json` 로컬 전용화
- 그간 git 추적됐던 허가 목록을 `.gitignore`에 추가 + `git rm --cached`
- 커밋 `ad708cc` (main, 로컬 ahead 1) — **아직 origin 푸시 안 됨** (main direct push 정책 블록)
- xargs 일회성 명령 1건 잔존했지만 파일이 이제 로컬 전용이라 무해

### 3. `02. 대상자관리` 맥락 파악
- 단일 HTML + 5 JS, localStorage 기반 13 엔터티
- 5단계 대상자 선정 워크플로우 (`프로젝트_현황_2026-04-13.md`, `data_model_v2.md`)
- 출력 = 축소 CSV 4열 · 표준 CSV 11열 · XLSX
- **Supabase 코드 0건** — 완전 로컬 독립

### 4. Supabase 현황 스캔
- 놀랍게도 `cs_*` 테이블 + 5단계 선정 스키마가 **이미 DB에 구축**돼 있고 실데이터 존재 (80 targets / 5 batches / 81 contacts / 95 courses / 83 projects)
- 그러나 **`src/` 참조 0건** → 스키마만 있고 앱이 안 읽음
- 사용자 확인: 수기로 "다른 도구"가 지금 쓰는 중 (조사 필요)

### 5. 통합 설계 방향 결정 (→ ADR-001~003)
- Phase 1: 축소 CSV Import 브릿지 (최소 변경)
- Phase 2: `cs_*` 파이프라인 활성화 + 로컬 v2 저장 계층 Supabase 이관
- Phase 3 (BRIS 수집 서버 이관·로컬 폐기)은 본 스코프 밖

### 6. 장기 문서 체계 구축 (→ ADR-004)
- 위치: `survey_sd/docs/cs-integration/`
- `README.md` · `context.md` · `decisions.md` · `worklog/` 분리
- 세션 시작·종료 프로토콜 정의

## Decisions (상세는 decisions.md)
- ADR-001: Phase 1 + Phase 2 범위 확정
- ADR-002: 로컬 v2 HTML 툴 당분간 유지 (장기 흡수)
- ADR-003: `cs_*` 기존 데이터는 "다른 도구"와 공존
- ADR-004: 통합 문서는 `survey_sd/docs/cs-integration/`에

## Next
1. **Phase 1 상세 설계** (`phase1-spec.md`)
   - 축소 CSV 스키마 (4열) + survey_sd 필요 입력 스키마 매핑
   - 신규 엔드포인트 / server action
   - 신규 UI (대상자 import 페이지)
   - `distribution_batches`에 source 추적 컬럼 추가 필요성 검토
   - 마이그레이션 계획
2. **cs_* 기존 데이터 출처 조사** (task #8)
   - 사용자에게 "다른 도구" 정체·쓰기 경로 확인
   - Phase 2 설계의 전제조건
3. **Phase 2 상세 설계** (`phase2-spec.md`) — Phase 1 완료 후 또는 병행

## Open Questions
- `cs_*`에 이미 있는 80 targets는 Phase 1에서 그대로 둘지 vs 읽기 전용 뷰를 써서 당장 참고할지?
- v2 축소 CSV에 `survey_id` 지정 방식 — import 때 사용자가 드롭다운 선택? 파일 안에 포함?
- 6개월 중복 체크를 Phase 1에서 survey_sd측에서도 한번 더 할지, v2 단에서만 하고 앱은 신뢰할지?

---

## 추가 조사 (같은 날, `참고/` 자료 반영 후)

### 결정적 발견
**`02. 대상자관리/참고/cs_dashboard.html` 을 직접 읽어본 결과**:
- 이것이 사용자가 말한 "다른 도구"
- Supabase anon 키 직접 사용 (`gdwhbacuzhynvegkfoga`)
- `cs_*` 테이블과 뷰들(`v_cs_batch_dashboard`, `v_cs_target_detail`, `v_cs_dispatch_summary` 등) 읽고 씀
- **`dispatchSurvey()`(:1694) 가 이미 `https://exc-survey.vercel.app/api/distributions/cs-bridge` 호출하도록 작성돼 있지만 survey_sd에 엔드포인트 없음** → 수기 CSV 운영의 직접 원인
- `BRIDGE_KEY` 는 placeholder (`__SET_CS_BRIDGE_API_KEY__`)
- cs_survey_targets에 writeback 기대 필드: `distribution_id`, `survey_token`, `survey_url`, `dispatched_at`, `dispatch_channel`, `dispatch_error` — **현재 테이블에 미존재**

### Phase 범위 재정의
Phase 1 = **cs-bridge API 구현** (1-3일, 기존 예상보다 작음).
- Phase 1-a: bridge API + cs_survey_targets 확장 컬럼 + distribution_batches.source + cs_target_batches.survey_id
- Phase 1-b (후순위): CSV import UI

Phase 2 = **survey_sd 안으로 대상자 관리 흡수** (약 2주).
- /admin/cs-targets 섹션
- Prisma 스키마 반영 (기존 B안 활용 검토)
- 응답 writeback
- RLS 재정비

### 새 결정
- ADR-003 → ADR-005로 supersede (다른 도구 = cs_dashboard.html 식별)
- ADR-005: cs-bridge 계약 존중하여 구현
- ADR-006: Phase 1 = cs-bridge 우선

### 새 문서
- `phase1-spec.md` — Bridge API 완전 명세 (9개 섹션)
- `phase2-spec.md` — /admin/cs-targets UI + Prisma + writeback (10개 섹션)
- `context.md` · `decisions.md` 갱신

### 새 Open Question / 리스크
- (R1) anon 키로 cs_survey_targets 확장 컬럼 write 시 RLS 허용 여부 → 마이그레이션 후 테스트 필요
- (R3) 동일 targetId 중복 발송 race → unique 제약 고려
- (Q1) 배치당 survey 1:1? 또는 target별 override 가능? → 기본 1:1, Phase 2에서 override
- Phase 2 착수 전 `claude/romantic-lederberg` (Prisma B안) 상태 점검 필요
- 사내망 BRIS 접근은 Phase 3로 이월 (본 스코프 밖)

---

## 추가 3 — B안 점검 + Phase 1 runbook 작성

### B안 상태 (`claude/romantic-lederberg`)
- 2 commits (3fa1a2f, 34da848), **코드 변경 0 · 문서만**
- `AGENTS.md` 업데이트 + `docs/prisma-schema-sync-plan.md` 계획서
- 계획: Prisma schema 에 45+ 모델 추가 (45~60분 + 검증 20분)
- **미실행 상태**

### AGENTS.md 원칙 발견 (branch only)
> "새로 만든 테이블은 Prisma 모델을 추가하지 말고 Supabase client로 접근한다"
`cs_*` 전체가 Non-Prisma 목록에 포함.

### 결정 (ADR-007)
- `cs_*` 은 Supabase client 로 접근 — Prisma 모델 추가 안 함
- B안 = 독립 별건. Phase 1·2 와 분리
- Phase 2 예상 공수 2일 단축 (Prisma 작업 제외)

### phase1-runbook.md 작성
12 섹션, 8 Step 실행 순서:
- Step A: 브랜치·환경 (15분)
- Step B: DB 마이그레이션 (30분)
- Step C: Bridge API 구현 (3~4시간)
- Step D: 단위 테스트 (2시간)
- Step E: Preview 배포 + 수동 테스트 (1시간)
- Step F: E2E Playwright (2시간)
- Step G: Production 롤아웃 (30분)
- Step H: 문서화 (1시간)

총 예상 **2-3일 (실작업 1-2일 + 검증 1일)**.

### 실행 중 결정 필요 (D1-D5)
- D1: survey_id 지정 UI (초기: SQL update 수동)
- D2: DistributionBatch.source Prisma 모델 반영 여부
- D3: unique 제약 (distributions recipient_email, cs_survey_targets distribution_id)
- D4: 테스트 러너 선택
- D5: cs_dashboard.html 배포 위치

### Phase 2 변경
- §2 Prisma 스키마 반영 → "Supabase TS 타입 생성" 으로 교체
- 예상 공수 2주 → 1.5주
