# 2026-04-20 · Phase 2 Prep — Preflight + Key Rotation

**Goal**: Phase 2 착수 전 kickoff §3 preflight 점검 + 발견 이슈 정리. 코드 변경 없음.

## Done

### Preflight 점검
- `git log -5` main HEAD = `a6e3a1f` (kickoff 문서 `e24a6a0` 대비 PR #74 Phase 2 kickoff docs 1 커밋 앞, 정상)
- 작업 트리 = 추적 외 파일 3종뿐 (문서/CSV/폴더), 코드 변경 0
- Supabase `cs_*` 행수 = kickoff 시점과 동일 (80 targets · 5 batches · 2 dispatch · 5 participation · 0 external). 드리프트 없음.
- Phase 1 이후 **실 발송 0건** (`distribution_batches source='cs-bridge'` = 0, 동 distributions = 0). kickoff §5 Option B 미수행.

### Phase 1 E2E 회귀 실패 → 원인 규명
최초 `npx playwright test e2e/cs-bridge.spec.ts` 결과 **5/8 실패**, 전부 401.

진단:
- 401 no-key / 401 wrong-key / 405 GET (3건) 통과 = 서버 reachable, 키 불필요 케이스 정상
- 400 / 404 / 200 happy / 200 skipped / DB writeback (5건) 실패 = 전부 **정상 키를 보냈는데 401**
- 원인: **로컬 `.env.local` `CS_BRIDGE_API_KEY` ≠ Vercel production env 값**
- 진단 방법: `.env.local` 값(node dotenv로 길이/공백만 확인, 평문 미노출) + `cs_dashboard.html` line 679 하드코드 값으로 각각 production POST → `dashboard key=400 OK`, `local key=401`
- 결론: production + cs_dashboard.html은 이미 동기 (Phase 1 Step G). 로컬만 stale.
- 회귀가 아님 — Phase 1 머지 커밋의 "E2E 8/8"은 localhost dev server 대상이었고, production은 키가 매치하지 않아도 통과하는 smoke 4/4만 검증했음.

### CS_BRIDGE_API_KEY 회전 (3곳 동기)
사용자 승인 하에 전체 회전 선택 (clean slate).

**새 값**: 64-hex, `node crypto.randomBytes(32).toString('hex')` 생성. 본 worklog에는 기록하지 않음.

**적용**:
1. `.env.local` — `CS_BRIDGE_API_KEY=...` 라인 replace (node regex, 기존값 평문 미노출)
2. `02. 대상자관리/참고/cs_dashboard.html` line 679 `BRIDGE_KEY = '...'` Edit
3. Vercel Dashboard → Settings → Env Vars → `CS_BRIDGE_API_KEY` (Production) 값 교체 (사용자 수동)
4. Vercel Dashboard → Deployments → 최신 production → Redeploy (사용자 수동, `dpl_B8dj9pFuoDutFFpUTFVwcD5j5Ptr`)

**검증**:
- curl production: `new_key=400` (키 수락), `old_key=401` (폐기됨)
- `npx playwright test e2e/cs-bridge.spec.ts` → **8/8 passed (13.8s)** against production

### Option B 실행 시 안전 이슈 발견
현재 dispatch 가능한 유일 후보 = 배치 `cc14004b` ("테스트_4월2차"):
- eligible + step5_confirmed + 미발송 2건
- 수신자: 박현진(@join.co.kr), 김정수(@mma.go.kr) — **실 고객 이메일**

위험 경로:
- Bridge는 distributions만 생성 (status='pending')
- 매일 09:00 KST cron (`/api/cron/send-emails`)이 pending distribution 픽업 → 실 메일 송신
- Dashboard에서 "설문 발송" 누르고 09:00 전까지 미조치 시 **실 고객에게 테스트 설문 메일 발송**

모든 배치의 `cs_target_batches.survey_id = NULL` (O2 상태 그대로) — bridge가 400으로 막긴 하지만, 우회해 survey_id 세팅하면 바로 실 발송 경로.

대응안 3가지 (다음 세션 결정):
- **B1**: fake `@example.com` 수신자로 신규 안전 배치 seed → dashboard 클릭
- **B2**: E2E 8/8을 Option B 대체로 간주 + surveyUrl 브라우저 smoke 1건만 추가
- **B3**: `cc14004b`로 진행 + 발송 직후 distribution `status='cancelled'` (타이밍 민감)

추천: **B2** (E2E가 이미 같은 코드 경로 전부 커버, 추가 수작업 최소화).

## Open Items (Phase 2 본작업으로 이월)

| # | 항목 | 상태 | 해결 위치 |
|---|---|---|---|
| O1 | Vercel env `NEXT_PUBLIC_APP_URL` literal `\n` | 코드 방어 중 (route.ts strip), 근본 정정 미완 | Vercel Dashboard 5분 수작업 |
| O2 | `cs_target_batches.survey_id` 지정 UI 부재 | 미해결 | Phase 2 `/admin/cs-targets/[batchId]` 상세 페이지에 추가 |
| O3 | cs_dashboard.html anon RLS가 확장 컬럼 update 허용하는지 실전 검증 | 미수행 (Option B 보류) | B1 수행 시 자연 확인 |
| O4 | = O1 중복 | — | — |

## Decisions (append)
- **ADR 신규 없음**. 키 회전은 운영 hygiene, 설계 변경 아님.
- kickoff §5 "Option B → A 순서"는 유지하되 **Option B = E2E 8/8 + 브라우저 smoke**로 재정의 제안 (다음 세션 합의 필요).

## 남긴 산출물
- `worklog/2026-04-20-phase2-prep.md` (이 문서)
- `worklog/INDEX.md` 상단에 이 세션 줄 추가
- `phase2-kickoff-prompt.md` §1, §4, §5 업데이트 (현재 상태 반영)

## 다음 세션 시작점
`docs/cs-integration/phase2-kickoff-prompt.md` 그대로 진입. Preflight는 이미 끝났으므로 §5 첫 작업(Option B 경로 결정 → Option A `/admin/cs-targets` 착수)부터.
